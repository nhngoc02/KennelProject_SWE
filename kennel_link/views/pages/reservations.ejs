<%- include('../partials/emp-client-head', {title: 'Reservations', type: type}) %>

<body>
    <div class="container"> 
        <h2>Reservation Screen</h2>
        <% if(type == 'Client') { %>
            <a href="/reservations_search" class="button">Search My Reservations</a>
        <% } else { %>
            <a href="/reservations_search" class="button">Search Existing Reservations</a>
        <% } %>
        <a href="/res_add" class="button">Add a New Reservation</a>
    </div>
  </body>
  </html>